# Large media files - exceed GitHub's 100MB limit
*.mp4
*.mov
*.avi
*.mkv
*.wmv

# OS files
.DS_Store
Thumbs.db

# Environment — contains real credentials, NEVER commit
.env

# Logs (may contain PII / email content)
logs/mail-dev.log
logs/mail-failed.log
logs/rate-limit/

# Keep .htaccess files that protect the logs directory
!logs/.htaccess
!logs/rate-limit/.htaccess

# Local dev email logs (legacy entry kept for compatibility)
logs/

# Composer vendor directory
vendor/
